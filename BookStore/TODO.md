# TODO

- [ ] Replace UI using Bootstrap consistently across header/body/footer (ensure stylesheet + Bootstrap included correctly).
- [ ] Update `views/partials/header.ejs`, `views/index.ejs`, `views/inventory.ejs`, and forms to match the new UI design.
- [ ] Ensure `/css/styles.css` is loaded and selectors exist for all new classes.
- [ ] Run the app and verify UI changes on: Home, Inventory, Add, Edit, notifications.

